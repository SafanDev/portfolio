# Required for canonical URLs, Open Graph metadata, robots.txt and sitemap.xml.
# Use the final deployed HTTPS origin with no path and no trailing slash.
NEXT_PUBLIC_SITE_URL=https://portfolio.example.com
